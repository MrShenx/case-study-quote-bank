{
  "name": "score-analysis-studio",
  "version": "1.0.0",
  "private": true,
  "description": "Score Analysis Studio — the app itself is a single static index.html with no build step; this file exists only to declare the dependencies the api/ serverless functions need.",
  "dependencies": {
    "@vercel/kv": "3.0.0",
    "@vercel/blob": "2.8.0"
  }
}
